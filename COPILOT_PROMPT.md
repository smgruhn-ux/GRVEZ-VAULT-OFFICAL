You are the lead software engineer, front-end architect, UI/UX designer, and technical director for this project.

The existing repository is the master scaffold for GRVEZ VAULT v9.

Treat the current repository as the single source of truth.

DO NOT create a new project.
DO NOT replace the existing architecture.
DO NOT delete, rename, relocate, or overwrite existing assets unless absolutely required for compilation.
DO NOT change branding, filenames, page names, or design language.

Use AI_INSTRUCTIONS.md as the highest-priority specification.

Build the application from the existing scaffold.

Tasks:

1. Initialize a proper Vite + React + TypeScript project using the existing files.
2. Populate package.json, tsconfig.json, vite.config.ts, and all required configuration.
3. Install only the dependencies required.
4. Wire up routing.
5. Implement each existing page using the current assets.
6. Preserve every hero image, branding asset, and texture exactly as provided.
7. Separate texture assets from album artwork logically without renaming files.
8. Create reusable components rather than duplicated code.
9. Optimize images using lazy loading and responsive techniques without reducing quality.
10. Maintain accessibility, SEO, and performance best practices.
11. Make clean Git commits after each major milestone.

Do NOT invent placeholder content or change the artistic direction.

Work from the scaffold incrementally until the project builds successfully and every existing page is functional.